extends EventResource
class_name MuxEvent

@export var events: Array[EventResource]

func execute(grid_pos: Vector2) -> void:
	# TODO: Logic that would have the mediator pop up options to select between event resources
	pass
